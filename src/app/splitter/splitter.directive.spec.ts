import { SplitterDirective } from './splitter.directive';

describe('SplitterDirective', () => {
	it('should create an instance', () => {
		const directive = new SplitterDirective(null);
		expect(directive).toBeTruthy();
	});
});
